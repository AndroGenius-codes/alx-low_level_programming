#include "main.h"

/**                                               * main - tests function that prints if integer 0  *is positive or negative
*
* Return: 0
*/

int main(void)
{
        int i = 0;

        positive_or_negative(i);

        return (0);
}
